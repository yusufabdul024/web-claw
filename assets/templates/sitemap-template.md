# Sitemap — <Project Name>

**Phase:** 1.2 (Web Claw)
**Authored by:** UX Strategy Agent
**Based on:** `blueprint/discovery.md`
**Date:** <YYYY-MM-DD>

---

## Site graph

```
<Home>
├── <Page A>
│   └── <Sub-page A1>
├── <Page B>
├── <Page C>
└── <Legal>
    ├── Privacy
    └── Terms
```

---

## Page-by-page

### Page: Home — `/`

**Goal:** <one sentence, measurable. e.g., "Convert qualified visitors to trial signups; target signup-rate ≥ 4%.">
**Primary visitor:** <one line>
**Page-level CTAs:** Primary = `<label>`; Secondary = `<label>`; Rescue = `<label>` (footer / exit-intent if any).

**Sections (in order):**

1. **<Section name>** — Intent: <one line>. Copy artifact: <H1 text>. CTA: `<label>` (if any).
2. **<Section name>** — Intent: <…>. Copy artifact: <…>.
3. **<Section name>** — Intent: <…>. Copy artifact: <…>.
4. **<Section name>** **[SIGNATURE]** — Intent: <…>. Copy artifact: <…>.
5. **<Section name>** — Intent: <…>. Copy artifact: <…>.
6. **<Section name>** — Intent: <…>. Copy artifact: <…>.

**Cuts:**
- ❌ Removed: <section>. Reason: <one line>.
- ❌ Removed: <section>. Reason: <…>.

---

### Page: <Page A> — `/<path>`

**Goal:** <…>
**Primary visitor:** <…>
**CTAs:** <…>

**Sections (in order):**
1. **<Section name>** — <…>
2. **<Section name>** **[SIGNATURE]** — <…>
3. **<Section name>** — <…>

**Cuts:**
- ❌ Removed: <…>. Reason: <…>.

---

(Repeat the per-page block for every page in the site graph.)

---

## High-value flows

Three highest-value visitor paths from entry to outcome.

**Flow 1 — <name, e.g., "Cold visitor → trial">:**
```
Home (Hero) → Home (Three Promises) → Home (Pricing CTA) → Signup form → confirmation
```

**Flow 2 — <name>:**
```
<Entry> → <Step> → <Step> → <Outcome>
```

**Flow 3 — <name>:**
```
<…>
```

---

## Sitemap-level cuts

Pages or sub-flows considered and not built:

- ❌ Removed: <e.g., "About page">. Reason: <e.g., "No outcome value; team photos go on Home contact section.">
- ❌ Removed: <…>. Reason: <…>.

---

## Open questions for the user

If anything in the sitemap is uncertain, list it here for the user to resolve before proceeding to Style Guide.

1. <Question>
2. <…>

---

## Sign-off

- [ ] User has reviewed the page list and confirmed.
- [ ] User has reviewed the signature-section choice per page.
- [ ] User has reviewed the cuts and accepted them.
- [ ] Ready to begin Phase 1.3: Style Guide.
